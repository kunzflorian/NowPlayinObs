#!meta

{"kernelInfo":{"defaultKernelName":"csharp","items":[{"name":"csharp","languageName":"csharp"},{"name":"fsharp","languageName":"F#","aliases":["f#","fs"]},{"name":"html","languageName":"HTML"},{"name":"http","languageName":"HTTP"},{"name":"javascript","languageName":"JavaScript","aliases":["js"]},{"name":"mermaid","languageName":"Mermaid"},{"name":"pwsh","languageName":"PowerShell","aliases":["powershell"]},{"name":"value"}]}}

#!csharp

// nuget / usings
#r "nuget: HtmlAgilityPack"
using System.IO;
using System.Net.Http;
using System.Text.Json.Nodes;
using HtmlAgilityPack;

#!csharp

var url = "https://raidpal.com/en/event/172-fryday-crew-dnb-raid-train-20-feb-2026";
var client = new HttpClient();

#!csharp

var response = await client.GetAsync(url);

if (!response.IsSuccessStatusCode)
        throw new Exception($"{response.StatusCode}: {response.ReasonPhrase}");

var html = await response.Content.ReadAsStringAsync();

var doc = new HtmlDocument();
doc.LoadHtml(html);

// Select all ld+json script tags
var jsonScripts = doc.DocumentNode
    .SelectNodes("//script[@type='application/ld+json']");

if (jsonScripts != null)
{
    foreach (var script in jsonScripts)
    {
        var json = script.InnerText.Trim();
        json.Display();
        /*
        var doc = JsonDocument.Parse(json)!;
        var root = doc.RootElement;

        if (root.TryGetProperty("performer", out var performerElement))
        {
                if (performerElement.ValueKind == JsonValueKind.Array)
                {
                        foreach (var performer in performerElement.EnumerateArray())
                        {
                                //performer.Display();
                                Console.WriteLine( performer.GetProperty("name").GetString());
                        }
                }                
        }


        root.Display();
        */
    }
}

html.Display();

return;

//var title = (string) currentTrack["title"];
//var artist = (string) currentTrack["artist"];
//var label = (string) currentTrack["label"];

//var info = $"{title} - {artist} - {label}";

//info.Display();

#!csharp

var response = await client.GetAsync(url);

if (!response.IsSuccessStatusCode)
        throw new Exception($"{response.StatusCode}: {response.ReasonPhrase}");

var html = await response.Content.ReadAsStringAsync();

var doc = new HtmlDocument();
doc.LoadHtml(html);

#!csharp

html.Display();
